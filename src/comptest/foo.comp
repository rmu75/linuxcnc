component foo "Compute the derivative of the input function";
pin in float in;
pin out float out;
variable double old;
function _;
license "GPL"; // indicates GPL v2 or later
;;
float tmp = in;
out = (tmp - old) / fperiod;
old = tmp;
//gcc -I ../rtapi/ -I../hal/ -L../../lib/ -DRTAPI foo.c -llinuxcnchal -lrt -o foo