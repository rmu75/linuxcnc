include "emc_common.fbs";

namespace EMC;

table OperatorError {
  error: string;
}

table OperatorText {
  text: string;
}

table OperatorDisplay {
  display: string;
}

union Message {
  operator_error: OperatorError,
  operator_text: OperatorText,
  operator_display: OperatorDisplay
}

table ErrorChannelMsg {
  message: Message;
}

root_type ErrorChannelMsg;
